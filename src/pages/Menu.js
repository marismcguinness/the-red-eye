import React from 'react';
import Typography from '@mui/material/Typography';

const Menu = () => {
    return (
        <div style={{ width: '100%', height: '100vh', backgroundColor: '#E3CDA0'}}>
            <Typography variant="h3" style={{ padding: 20,fontFamily: 'Special Elite', color: "#6C250D" }}>Menu</Typography>
        </div>
    );
}

export default Menu;